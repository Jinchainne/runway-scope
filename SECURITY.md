# Security Policy

RunwayScope is not a dispatch, clearance, safety, legal, or compensation system. Results are scoped to the frozen identity and snapshots fetched during consensus. Source operators can publish incomplete or incorrect information; `UNRESOLVED` is the safe path when the required evidence cannot be bound.

Report vulnerabilities with the affected method, reproduction, expected behavior, and impact through GitHub private vulnerability reporting. Never include wallet secrets or active credentials.
